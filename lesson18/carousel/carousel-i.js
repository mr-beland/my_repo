const $controlElements = document.querySelectorAll('.controls')
const $carouselContainer = document.querySelectorAll('.carousel-container')

let conrolsClickHandler = (e) => {
    //что делаем, когда получили клик
    $carouselContainer
} 

$controlElements.forEach(Element => {

    // тут происходят итерации цикла forEarch - перебор массива controlElements
    element.addEventListener(
        'click',
        (e) => {
            // что делаем, когда получили клик
        }
    )

})

// for (let element of $controlElements) {

//     // тут происходят итерации цикла forEarch - перебор массива controlElements

// }

